<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="Theme.Base" parent="AppTheme">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowBackground">@color/Background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Theme.Outbreaks" parent="Theme.Base"/>


    <style name="ActionBarOverlay" parent="">
        <!-- Changing Text Color-->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="colorControlHighlight">@color/Background</item>
        <item name="android:actionMenuTextColor">@color/white</item>
        <!-- TextColor secondary colors action Menu(Literally the icon)-->
        <item name="android:textColorSecondary">@color/white</item>
    </style>

    <style name="ActionBarPOP" parent="">
        <item name="android:background">@color/colorPrimary</item>
    </style>
    <style name="ActionBar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>


</resources>
